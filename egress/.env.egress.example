# LiveKit Egress worker configuration

# Required: point to your self-hosted LiveKit
LIVEKIT_WS_URL=wss://ai.acropaq.com
LIVEKIT_API_URL=https://ai.acropaq.com

# Required: API key/secret matching your LiveKit server
LIVEKIT_API_KEY=REPLACE_ME
LIVEKIT_API_SECRET=REPLACE_ME

# Choose ONE output target:
# 1) Local file output (quick start). Files saved under ./output on host
FILE_OUTPUT_LOCAL=/var/lib/livekit-egress-out

# 2) S3 (recommended for production)
#AWS_ACCESS_KEY_ID=REPLACE_ME
#AWS_SECRET_ACCESS_KEY=REPLACE_ME
#AWS_REGION=eu-west-1
#S3_BUCKET=your-bucket
#S3_PREFIX=egress

# Optional logging
LK_LOG_LEVEL=info

