server {
  listen 80;
  server_name ai.acropaq.com;
  return 301 https://$host$request_uri;
}

server {
  listen 443 ssl http2;
  server_name ai.acropaq.com;

  ssl_certificate     /etc/letsencrypt/live/ai.acropaq.com/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/ai.acropaq.com/privkey.pem;

  client_max_body_size 10m;

  # Backend app (serves /ui and APIs)
  location / {
    proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_read_timeout 3600;
  }

  # LiveKit
  location /rtc {
    proxy_pass http://127.0.0.1:7880;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_read_timeout 86400;
  }

  # LiveKit Admin (Twirp) — do not strip path
  # Example: /twirp/livekit.RoomService/CreateRoom
  location /twirp {
    proxy_pass http://127.0.0.1:7880;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 60s;
  }
}
